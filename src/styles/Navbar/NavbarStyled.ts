import styled from "styled-components";

export const NavbarStyled = styled.nav`
  color: var(--primary);
  font-size: 2rem;
  font-weight: 600;
  padding: 1rem;
  
  span {
    font-weight: 500;
  }
`;
